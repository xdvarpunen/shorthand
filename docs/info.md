## encoder decoder
using enum as identifier currently


## Folder Structure

```bash
/docs # documentation for project
/lib
    /toolbox
        /core # stuff kept simple with `List<List<Point>>` type
        /model # types communicate with
        /toolbox # classes services use to process strokes
    /ui
        /pages # pages
        /templates # reusable page layouts
        /widgets # reusable widgets
    app.dart # bootstrap
    main.dart # starting point for app
```

- pages based on template

## TODO

turn main page into list view too
add clipboard
canvas to oop